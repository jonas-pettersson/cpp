/*
 * File: MergeSort.cpp
 * -------------------
 * This program reads a file of integers and sorts
 * them using the merge sort algorithm.
 */

#include <iostream>
#include <string>
#include <fstream>
#include "vector.h"
using namespace std;

const string FILENAME = "../IntegerArray.txt";

/* Main program */

int main() {
  Vector<int> vec;
  /*  ifstream infile(FILENAME);
  int a;
  while (infile >> a) {
      vec.add(a);
      } */
  return 0;
}
